Welcome to SimpleDS
===================

SimpleDS provides a simple Java-to-Entity framework that maps java classes
to persistent entities, getting as little in the way as possible. 

Distribution
============

The distribution file includes the following:

 * simpleds.jar 			The jar with java classes
 * simpleds-javadoc.jar 	The javadoc jar file
 * simpleds-sources 		The jar file with all sources
 * lib/						The third-party libraries required to use simpleds
 
Compile
=======

If you are working from the sources, execute "gradle eclipse"
 